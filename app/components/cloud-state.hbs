<FaIcon @icon="cloud-upload-alt" class={{if this.isCloudSynced "text-success"}} />
<FaIcon @icon="cloud-download-alt" class={{if this.isLocalSynced "text-success"}} />
<FaIcon @icon="circle" class={{if this.isOnline "text-success"}} />