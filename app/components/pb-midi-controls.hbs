  <BsForm
    class="row g-0 bg-dark"
    @formLayout="horizontal"
    @model={{this.midi}}
    as |form|
  >
    <BsButton
      class="btn-sm col-auto text-truncate border rounded-0"
      @type={{if this.noFilters "secondary" "success"}}
      @onClick={{this.toggleMute}}
      disabled={{(not @isConnected)}}
      title="Toggle chords"
    >{{svg-jar "midi" class="svg-inline--fa" width="1em" fill="white"}} Midi</BsButton>
    <BsButton
      class="btn-sm col text-truncate border rounded-0"
      @type={{if this.noChords "secondary" "danger"}}
      @onClick={{this.toggleChords}}
      disabled={{this.noFilters}}
      title="Toggle chords"
    ><FaIcon @icon="cancel" /> Chords {{this.midi.chordsEnabled}} {{this.noChords}}</BsButton>
    <BsButton
      class="btn-sm col text-truncate border rounded-0"
      @type={{if this.noNotes "secondary" "danger"}}
      @onClick={{this.toggleNotes}}
      disabled={{this.noFilters}}
      title="Toggle notes"
    ><FaIcon @icon="cancel" /> Notes {{this.midi.notesEnabled}} {{this.noNotes}}</BsButton>
    <form.element
      @controlType="power-select"
      @property="selectedKeyOption"
      @selected={{this.midi.selectedKeyOption}}
      @onChange={{this.setMidiKey}}
      @formLayout="inline"
      class="col border-light"
      @allowClear={{true}}
      @options={{this.midi.availableKeys}}
      required
      as |type|
    >
      <type.control
        @placeholder="Select key"
        as |option|
      >
        {{option}}
      </type.control>
    </form.element>
    <form.element
      @controlType="power-select"
      @property="selectedModeOption"
      @selected={{this.midi.selectedModeOption}}
      @onChange={{this.setMidiMode}}
      @formLayout="inline"
      class="col border-light"
      @allowClear={{true}}
      @options={{this.midi.availableModes}}
      required
      as |type|
    >
      <type.control
        @placeholder="Select mode"
        as |option|
      >
        {{option}}
      </type.control>
    </form.element>
  </BsForm>