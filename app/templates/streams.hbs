<PbStreams @streams={{this.model}} @queryParamsObj={{this.queryParamsObj}} @isViewing={{this.isViewing}} @createStream={{this.createStream}} @closeStream={{this.closeStream}} @loadStream={{this.loadStream}}>{{outlet}}</PbStreams>
