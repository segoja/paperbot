<PbCommands @commands={{this.model}} @queryParamsObj={{this.queryParamsObj}} @isViewing={{this.isViewing}} @closeCommand={{this.closeCommand}} @loadCommand={{this.loadCommand}} @createCommand={{this.createCommand}}>{{outlet}}</PbCommands>
