<PbTimers 
  @timers={{this.model}} 
  @queryParamsObj={{this.queryParamsObj}}
  @timerTypes={{this.timerTypes}}
  @createTimer={{this.createTimer}} 
  @importTimers={{this.importTimers}} 
  @gridDeleteTimer={{this.gridDeleteTimer}}
  @gridEditTimer={{this.gridEditTimer}}
  @gridActiveTimer={{this.gridActiveTimer}}
>
  {{outlet}}
</PbTimers>
