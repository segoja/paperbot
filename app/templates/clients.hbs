<PbClients @clients={{this.model}} @queryParamsObj={{this.queryParamsObj}} @isViewing={{this.isViewing}} @closeClient={{this.closeClient}} @loadClient={{this.loadClient}} @createClient={{this.createClient}}>{{outlet}}</PbClients>
