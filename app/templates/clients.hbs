<PbClients 
@clients={{this.model}} 
@queryParamsObj={{this.queryParamsObj}} 
@isViewing={{this.isViewing}} 
@createClient={{this.createClient}}
@closeClient={{this.closeClient}}
@gridDeleteClient={{this.gridDeleteClient}}
@gridEditClient={{this.gridEditClient}}
@importClients={{this.importClients}}

>{{outlet}}</PbClients>
