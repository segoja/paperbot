<PbClient
  @client={{this.model}}
  @clientTypes={{this.clientTypes}}
  @saveClient={{this.saveClient}}
  @saveAndReturnClient={{this.saveAndReturnClient}}
  @editClient={{this.editClient}}
  @closeClient={{this.closeClient}}
  @deleteClient={{this.deleteClient}}
/>