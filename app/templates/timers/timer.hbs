<PbTimer 
  @timer={{this.model}}
  @timerTypes={{this.timers.timerTypes}}
  @saveTimer={{this.saveTimer}}
  @saveAndReturnTimer={{this.saveAndReturnTimer}}
  @editTimer={{this.editTimer}}
  @closeTimer={{this.closeTimer}}
  @deleteTimer={{this.deleteTimer}}
 />
