<PbStream 
@stream={{this.model}} 
@messages={{this.messages}}
@clients={{this.streams.clients}} 
@overlays={{this.streams.overlays}} 
@commands={{this.streams.commands}} 
@songs={{this.streams.songs}} 
@isEditing={{this.isEditing}} 
@editStream={{this.editStream}}  
@closeStream={{this.closeStream}} 
@setOverlay={{this.setOverlay}}
@setBotClient={{this.setBotClient}}
@setChatClient={{this.setChatClient}}
@overlayGenerator={{this.overlayGenerator}}
@saveStream={{this.saveStream}} 
@saveAndReturnStream={{this.saveAndReturnStream}} 
@deleteStream={{this.deleteStream}} />
